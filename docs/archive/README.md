# docs/archive — historical docs (as-built record)

Superseded but kept for provenance. **Not** active references — see `docs/README.md` for the live set.

- `ARTHAYANTRA_2_FEATURE_PROPOSALS.md` — the 2026-06-12 owner feature selection and source of the
  `[FP-N]` tags cited in `docs/design/`. Its Mandatory/Recommended items became ADR amendments
  A7–A12 and the letter-suffixed phases; only the "Future" titles remain (the master plan will
  plan those separately). Kept so the `[FP-N]` breadcrumbs in the design docs still resolve.
