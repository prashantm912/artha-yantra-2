# Market-open signal-liveness gate — 2026-07-21 (data date)

Analysis date: 2026-07-21, ran 09:44–09:49 IST (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (the §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-21-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint.

---

## 0 Verdict

**PASS** — the live SignalEngine is evaluating. Σ `ay_signal_eval_outcome_total` advanced **+36 in
~4.5 min** while bars were arriving, `ay_signal_eval_failures_total` = 0, and evaluations are
reaching the *confluence* stage (not parked at the chart gate). 30 rejection rows by 09:46 IST,
0 fires.

## 1 Preconditions

| check | result |
|---|---|
| Trading day / in-session | Tue 2026-07-21, DB clock **09:44 IST**, inside 09:15–15:30. No July-2026 row in `libs/market-calendar/.../nse-trading-holidays.csv`. |
| Stack | 11/11 containers healthy (up ~55 min, booted ~08:50 IST). |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-21 09:44:23 IST** (today). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-21T04:15:01Z","tickedTokens":25,"problems":[]}` (04:15Z = 09:45 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **29 bars** each on `NIFTY26JULFUT` / `NIFTY26AUGFUT` / `NIFTY26SEPFUT`, max bucket **09:43 IST** (= now−1m). Count tracks minutes-since-open. |

## 2 THE GATE — outcome counters (actuator :8082)

`docker exec ay-strategy-signal-service sh -c "wget -qO- http://127.0.0.1:8082/actuator/prometheus | grep -E 'ay_signal_eval_outcome|ay_signal_eval_failures'"`

| outcome | R1 @ 04:14Z (09:44 IST) | R2 @ 04:18:40Z (09:48:40 IST) | Δ |
|---|---|---|---|
| `chart-gate-failed` | 18 | 38 | +20 |
| `composite-below-threshold` | 4 | 4 | 0 |
| `confluence-blocked` | 14 | 30 | +16 |
| `confluence-gate-absent` | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 |
| `unscoreable-indicators-warming` | 0 | 0 | 0 |
| **Σ** | **36** | **72** | **+36** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 |

Counters are in-memory and reset on restart — only the **delta** is meaningful. Readings taken
≥3 min apart (one 3m bar boundary).

**Why counters and not `signal_rejections`:** `recordRejection`'s two call sites
(`SignalEngine:1228`, `:1406`) are both downstream of the `chart != Outcome.FIRED` early return at
`SignalEngine:1132-1134`, so only the `confluence-blocked` outcome writes a row. Chart-gate
failures, below-threshold composites and warming indicators leave no row and no log. An empty
rejections table is therefore *not* evidence of a dead engine — that premise produced the false
starvation alarm on 2026-07-17 and an unnecessary live-service restart on 2026-07-20.

Here the two agree anyway: `confluence-blocked` = 30 at R2, and the rejections table held exactly
30 rows.

## 3 Session shape so far (observation, no action)

`strategy.signal_rejections` since 09:15 IST — **30 rows, max `09:46:21 IST`**, 0 fires.
Blocking-rail mix at the 14-row mark:

| blocking_rail | rows |
|---|---|
| `time-window` | 8 |
| `volume-floor` | 3 |
| `option-side-constraint` | 2 |
| `morning-opening-formation` | 1 |

Normal early-session shape (opening-formation and time-window rails are expected to dominate the
first ~30 min). Nothing here justifies a tune off one 30-minute window.

## 4 Dot health

`GET /api/v1/signal-rejections/dot-health` (rowsInspected 14, `session:true`):

| dot | required | alive |
|---|---|---|
| `breadth` | yes | **live** |
| `vix` | no | live |
| `oi_spurt_price` | no | live |
| `iv_rank` | no | **dead** |
| `dow` | no | **dead** |
| `fii` | no | **dead** |

All three dead dots are **optional** and the pattern is pre-existing (dead-dot recalibration
#675/#676). Flagged for the evening post run to compare against the 07-20 ledger — notably 07-20's
headline was all three **OI** dots dead (`futures_oi`, `underlying_oi`, `oi_spurt`); today
`oi_spurt_price` reads live at the 14-row mark, which the post run should confirm across the full
session before calling it resolved.

## 5 Carry into the evening `post` run

1. Confirm whether the 07-20 all-OI-dots-dead finding recurs today (early read says `oi_spurt_price`
   is live — verify over the full row population, not 14 rows).
2. `iv_rank` / `dow` / `fii` dead — is this every session (structural) or day-dependent (regime)?
3. Fold this gate's PASS + counter deltas into `2026-07-21-session-findings.md` as the opening
   datapoint.
