# Market-open signal-liveness gate — 2026-07-22 (data date)

Analysis date: 2026-07-22, ran 09:45–09:56 IST (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (README §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-22-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint.

---

## 0 Verdict

**PASS** — the live SignalEngine is evaluating. Σ `ay_signal_eval_outcome_total` advanced **+72 in
~5 min** while bars were arriving, `ay_signal_eval_failures_total` = 0, and evaluations reach the
*confluence* stage (not parked at the chart gate). 50 rejection rows across 16 slugs by 09:50 IST
(82 rows by 09:55), 0 fires.

## 1 Preconditions

| check | result |
|---|---|
| Trading day / in-session | Wed 2026-07-22, host clock **09:45 IST**, inside 09:15–15:30. No July-2026 row in `libs/market-calendar/src/main/resources/nse-trading-holidays.csv`. |
| Stack | 11/11 containers healthy (up ~47 min, booted ~08:58 IST). |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-22 09:43:23 IST** (today). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-22T04:15:28Z","tickedTokens":25,"problems":[]}` (04:15Z = 09:45 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **30 bars** each on `NIFTY26JULFUT` / `NIFTY26AUGFUT` / `NIFTY26SEPFUT`, max bucket **09:44 IST** (= now−1m). Count tracks minutes-since-open. |

## 2 THE GATE — outcome counters (actuator :8082)

`docker exec ay-strategy-signal-service sh -c "wget -qO- http://127.0.0.1:8082/actuator/prometheus | grep -E 'ay_signal_eval_outcome|ay_signal_eval_failures'"`

| outcome | R1 (09:45:2x IST) | R2 (09:47:05) | R3 (09:50:16) | Δ R1→R3 |
|---|---|---|---|---|
| `chart-gate-failed` | 18 | 38 | 58 | +40 |
| `composite-below-threshold` | 0 | 0 | 0 | 0 |
| `confluence-blocked` | 18 | 34 | 50 | +32 |
| `confluence-gate-absent` | 0 | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 | 0 |
| `unscoreable-indicators-warming` | 0 | 0 | 0 | 0 |
| **Σ** | **36** | **72** | **108** | **+72** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 | 0 |

Counters are in-memory and reset on restart — only the **delta** is meaningful. R1→R3 spans
≥3 min (one 3m bar boundary). Nothing sits in `unscoreable-indicators-warming`, so indicator
warm-up is complete and every evaluation reaches a real verdict.

**Why counters and not `signal_rejections`:** `recordRejection`'s two call sites
(`SignalEngine:1228`, `:1406`) are both downstream of the `chart != Outcome.FIRED` early return at
`SignalEngine:1132-1134`, so only the `confluence-blocked` outcome writes a row. Chart-gate
failures, below-threshold composites and warming indicators leave no row and no log. An empty
rejections table is therefore *not* evidence of a dead engine — that premise produced the false
starvation alarm on 2026-07-17 and an unnecessary live-service restart on 2026-07-20.

Here the two agree: `confluence-blocked` = 50 at R3 and the rejections table held exactly 50 rows
for the same window.

## 3 Cold-start strategy load (observation, no action)

Boot logs, read the same day (§3.10 — a restart destroys them):

```
03:28:23Z  signal engine loaded 0 published strategies (38 dropped on an unresolved universe, 0 failed to load)   [main]
03:29:43Z  signal engine loaded 38 published strategies (0 dropped on an unresolved universe, 0 failed to load)   [signal-eval]
```

= 08:58:23 IST cold-boot loads **zero** (Kite login not yet done), then the 08:59:43 IST re-resolve
on the eval thread loads **38 with `unresolved == 0`**. That is the F10 Part-A cold-start retry
(#874) working exactly as designed; the honest health signal is `unresolved == 0`, never
`loaded > 0`, and it is satisfied. 38 loaded vs 44 published+enabled strategies = the 6 non-scalper
(swing) rows, which the scalper engine does not load.

Coverage sanity: **16 distinct slugs** emitting by 09:50 vs **18** in the same 09:15–09:50 window on
2026-07-21. No shrinkage of the 07-17 class.

## 4 Session shape so far (observation, no action)

`strategy.signal_rejections` since 09:15 IST — **82 rows, 16 slugs, max `09:55:11 IST`**, **0 fires**
(`strategy.signals` = 0 rows). `strategy.subscriber_health_events` = **0 rows** today (no eval-stall).

Blocking-rail mix at the 82-row mark:

| blocking_rail | rows |
|---|---|
| `volume-floor` | 52 |
| `time-window` | 18 |
| `time-of-day-preference` | 8 |
| `option-side-constraint` | 2 |
| `rsi-band` | 2 |

Normal early-session shape. `volume-floor` dominating the first 40 min is expected (thin opening
bars vs the banded floor) and matches the 07-21 session's leading rail — nothing here justifies a
tune off one 40-minute window, and §3.13 forbids judging that knob off anything but the
would-have-fired set anyway.

## 5 Dot health

`GET /api/v1/signal-rejections/dot-health` (rowsInspected 40, `session:true`, asOf 09:56:06 IST):

| dot | required | alive |
|---|---|---|
| `breadth` | yes | **live** |
| `vix` | no | live |
| `oi_spurt_price` | no | live |
| `iv_rank` | no | **dead** |
| `dow` | no | **dead** |
| `fii` | no | **dead** |

Same three dead optional dots as 07-21 — pre-existing, post-recalibration (#675/#676) pattern.

**OI quadrant liveness (§3.12) — the 07-20 defect is NOT recurring.** Of 82 rows:
`futuresQuadrant = 'NEUTRAL'` on **0** rows (07-20 was 748/748). Real quadrants populate:

| futuresQuadrant | underlyingQuadrant | rows |
|---|---|---|
| *(null)* | *(null)* | 30 |
| `SHORT_BUILDUP` | `LONG_BUILDUP` | 24 |
| `SHORT_BUILDUP` | `SHORT_BUILDUP` | 16 |
| `LONG_BUILDUP` | `LONG_BUILDUP` | 12 |

The 30 null-quadrant rows are rows blocked before the OI read (the `time-window` /
`time-of-day-preference` rails), not a dead feed — null ≠ NEUTRAL, and it is NEUTRAL that
`OiInterpretation.classify` can never legitimately emit.

## 6 Carry into the evening `post` run

1. Confirm across the full session that `futuresQuadrant` NEUTRAL stays at 0 — if so the 07-20
   all-OI-dots-dead finding was a one-session capture fault, and its ledger row can move.
2. `volume-floor` is again the leading first-rail. Judge it ONLY on the §3.5 would-have-fired set
   (§3.13), never the champion shadow book's per-rail bucket, and re-run the §3.14 published-config
   check that `relative-volume-floor` is still armed in the published versions.
3. `iv_rank` / `dow` / `fii` dead again — structural (every session) by now, not regime; the post
   run should say so explicitly and price the composite cap accordingly.
4. Fold this gate's PASS + counter deltas in as the session's opening datapoint.
