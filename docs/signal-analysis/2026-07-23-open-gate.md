# Market-open signal-liveness gate — 2026-07-23 (data date)

Analysis date: 2026-07-23, ran 09:35–09:41 IST (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (README §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-23-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint.

---

## 0 Verdict

**PASS** — the live SignalEngine is evaluating. Σ `ay_signal_eval_outcome_total` advanced **+8 in
~3.7 min** (24 → 32) across a 3m bar boundary while 1m bars were arriving,
`ay_signal_eval_failures_total` = 0, nothing parked in `unscoreable-indicators-warming`, and
evaluations reach the *confluence* stage. 8 rejection rows across 2 slugs by 09:40 IST, 0 fires.

## 1 Preconditions

| check | result |
|---|---|
| Trading day / in-session | Thu 2026-07-23, host clock **09:35:54 IST**, inside 09:15–15:30. No July-2026 row in `libs/market-calendar/src/main/resources/*.csv`. |
| Stack | 11/11 containers healthy (up ~58 min, booted ~08:38 IST). |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-23 09:28:02 IST** (today, user `KM8033`). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-23T04:06:00Z","tickedTokens":25,"problems":[]}` (04:06Z = 09:36 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **21 bars** each on `NIFTY26JULFUT` / `NIFTY26AUGFUT` / `NIFTY26SEPFUT`, max bucket **09:35 IST** (= now−1m). Count tracks minutes-since-open. |
| Eval-stall telemetry | `strategy.subscriber_health_events` since 00:00 IST: **0 rows**. |

## 2 THE GATE — outcome counters (actuator :8082)

`docker exec ay-strategy-signal-service sh -c "wget -qO- http://127.0.0.1:8082/actuator/prometheus | grep -E 'ay_signal_eval_outcome|ay_signal_eval_failures'"`

| outcome | R1 (~09:36:30 IST) | R2 (09:37:57) | R3 (09:40:10) | Δ R1→R3 |
|---|---|---|---|---|
| `chart-gate-failed` | 12 | 14 | 16 | +4 |
| `composite-below-threshold` | 8 | 8 | 8 | 0 |
| `confluence-blocked` | 4 | 6 | 8 | +4 |
| `confluence-gate-absent` | 0 | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 | 0 |
| `unscoreable-indicators-warming` | 0 | 0 | 0 | 0 |
| **Σ** | **24** | **28** | **32** | **+8** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 | 0 |

Counters are in-memory and reset on restart — only the **delta** is meaningful. R1→R3 spans ≥3 min
(the 09:37 and 09:40 3m bar closes). A re-read at 09:40:22 was flat vs 09:40:10, as expected
between bar boundaries.

**Why counters and not `signal_rejections`:** `recordRejection`'s two call sites
(`SignalEngine:1228`, `:1406`) are both downstream of the `chart != Outcome.FIRED` early return at
`SignalEngine:1132-1134`, so only the `confluence-blocked` outcome writes a row. Chart-gate
failures, below-threshold composites and warming indicators leave no row and no log. An empty
rejections table is therefore *not* evidence of a dead engine — that premise produced the false
starvation alarm on 2026-07-17 and an unnecessary live-service restart on 2026-07-20.

Here the two agree: `confluence-blocked` = 8 at R3 and the rejections table held exactly 8 rows for
the same window (max `generated_at` **09:40:01 IST**).

**Eval rate is on the pre-ramp baseline, not depressed.** Σ 32 over the 09:15–09:40 window ≈ 4
evals per 3m bar. 2026-07-22's gate saw 36 evals over its first ~10 bars (≈3.6/bar) before jumping
to ~36/bar from 09:45, when the bulk of the scalper time windows open. Today's number sits on that
same pre-ramp line; the midday gate should confirm the post-09:45 ramp actually happened.

## 3 Cold-start strategy load (observation, no action)

```
03:08:03Z (main)        signal engine loaded 0 published strategies (38 dropped on an unresolved universe, 0 failed to load)
03:09:06Z (signal-eval) signal engine loaded 38 published strategies (0 dropped on an unresolved universe, 0 failed to load)
```

The F10 cold-start shape (engine boots before the Kite login resolves the universe) appeared again
and the retry recovered ~63 s later. The health signal is `unresolved == 0`, never `loaded > 0` —
the final state satisfies it.

Coverage denominator: `strategy.strategies` published+enabled = **44** = **38 scalpers** + 6 swing
(batch-engine, not live-loaded). The engine's 38 is the full scalper set, so §3.10 coverage is
complete at the load layer. Only 2 slugs have *emitted* rows so far, which is the time-window
effect above, not a load failure.

## 4 Data-integrity probes

| probe | result |
|---|---|
| §3.15 misaligned (phantom) 1m candles since 09:15 | **0 rows** (07-22 had 308). Clean so far. |
| §3.12 futures-OI capture cadence (`NIFTY26JULFUT`, 09:15–09:40) | **13 distinct minutes** — vs 14 / 13 / 12 on 07-20 / 07-21 / 07-22 in the same window. Normal. |
| First-rail histogram (8 rows) | `time-window` 6, `option-side-constraint` 2. |

## 5 Dot health — reads all-dead, but the sample is context-less (do not alarm)

`GET /api/v1/signal-rejections/dot-health` at 09:40:36 IST, `rowsInspected: 8`: **all six probes
report `alive: false`**, including the REQUIRED `breadth` — which on 07-22 was live.

This is a **sampling artifact, not a feed regression.** All 8 of today's rejection rows so far are
blocked *before* the context is assembled (`time-window` × 6, `option-side-constraint` × 2) and
carry `diagnostic->'context' IS NULL` on 8 of 8. With no context rows in the window, every dot
probe necessarily reads dead. Sample sizes for the comparison are 8 rows (today) vs 40 rows
(07-22 at 09:56), so the two are not comparable.

**New data point (candidate README §3 dimension):** `DotHealthCanary`'s "newest N rejections"
sample can be composed entirely of context-less pre-gate rejections early in a session, at which
point it reports every dot — including required ones — as dead. Treat a dot-health all-dead reading
as **uninformative** until at least some rows in the window carry a non-null `context`; the check
is `count(*) FILTER (WHERE diagnostic->'context' IS NOT NULL)` over the same window. Worth a
follow-up: the canary currently pages on a newly-dead REQUIRED dot, so this shape is a
false-page risk (it did not page today — verify why before treating that as safe).

## 6 Carry into the evening `post` run

1. Re-read dot-health once the session has context-bearing rejections; confirm `breadth` / `vix` /
   `oi_spurt_price` are live and only `iv_rank` / `dow` / `fii` are dead (the structural trio).
2. Confirm the post-09:45 eval ramp (Σ per 3m bar rising from ~4 to the ~36 seen on 07-22) — a flat
   4/bar all session would be a real coverage problem that this gate is too early to see.
3. Confirm `futuresQuadrant = 'NEUTRAL'` stays at 0 across the full session (§3.12 carry from 07-22).
4. Re-run the §3.15 phantom-candle probe at EOD — zero at 09:40 only proves no feed outage yet.
5. Fold this gate's PASS + counter deltas in as the session's opening datapoint.
