# Market-open signal-liveness gate — 2026-07-24 (data date)

Analysis date: 2026-07-24, ran 11:02–11:05 IST (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (README §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-24-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint. (Run fired later than the usual 09:35 window;
it is a mid-morning liveness gate, ~1h50m into the session.)

---

## 0 Verdict

**PASS** — the live SignalEngine is evaluating. Σ `ay_signal_eval_outcome_total` advanced **+36 in
~3.5 min** (4542 → 4578) across a 3m bar boundary while 1m bars were arriving,
`ay_signal_eval_failures_total` = 0, nothing parked in `unscoreable-indicators-warming`, and
evaluations reach the *confluence* stage. 422 rejection rows by 11:04 IST, 0 fires — a quiet/down
tape, all scalpers sharing the one composite go silent together (normal, not a fault).

## 1 Preconditions

| check | result |
|---|---|
| Trading day / in-session | Fri 2026-07-24, host clock **11:02:06 IST**, inside 09:15–15:30. No July-2026 row in `libs/market-calendar/src/main/resources/*.csv`. |
| Stack | 11/11 containers healthy (up ~26h, no restart today). |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-24 10:58:31 IST** (today). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-24T05:32:25Z","tickedTokens":25,"problems":[]}` (05:32Z = 11:02 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **107 bars** each on `NIFTY26JULFUT` / `NIFTY26AUGFUT` / `NIFTY26SEPFUT`, max bucket **11:01 IST** (= now−1m). Count tracks minutes-since-open. |
| Eval-stall telemetry | `strategy.subscriber_health_events` since 00:00 IST: **0 rows**. |

## 2 THE GATE — outcome counters (actuator :8082)

`docker exec ay-strategy-signal-service sh -c "wget -qO- http://127.0.0.1:8082/actuator/prometheus | grep -E 'ay_signal_eval_outcome|ay_signal_eval_failures'"`

| outcome | T1 (~11:02:15 IST) | T2 (~11:05:45) | Δ |
|---|---|---|---|
| `chart-gate-failed` | 2530 | 2548 | +18 |
| `composite-below-threshold` | 178 | 178 | 0 |
| `confluence-blocked` | 1834 | 1852 | +18 |
| `confluence-gate-absent` | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 |
| `unscoreable-indicators-warming` | 0 | 0 | 0 |
| **Σ** | **4542** | **4578** | **+36** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 |

Counters are in-memory and reset on restart — only the **delta** is meaningful. T1→T2 spans ≥3 min
(one 3m bar close). Σ advancing = the eval loop is live and reaching the confluence stage.

**Why counters and not `signal_rejections`:** `recordRejection`'s two call sites
(`SignalEngine:1228`, `:1406`) are both downstream of the `chart != Outcome.FIRED` early return at
`SignalEngine:1132-1134`, so only the `confluence-blocked` outcome writes a row. Chart-gate
failures, below-threshold composites and warming indicators leave no row and no log. An empty
rejections table is therefore *not* evidence of a dead engine — that premise produced the false
starvation alarm on 2026-07-17 and an unnecessary live-service restart on 2026-07-20.

Here the two agree: `confluence-blocked` advanced +18 and the rejections table advanced +18
(404 → 422) over the same window, max `generated_at` **11:04:22 IST**.

## 3 Cold-start strategy load (observation, no action)

```
2026-07-23T03:08:03Z (main)        signal engine loaded 0 published strategies (38 dropped on an unresolved universe, 0 failed to load)
2026-07-23T03:09:06Z (signal-eval) signal engine loaded 38 published strategies (0 dropped on an unresolved universe, 0 failed to load)
```

Boot line is from the 07-23 08:38 IST boot (26h uptime, no restart today). The F10 cold-start shape
appeared and the retry recovered ~63 s later; final state `unresolved == 0` satisfies the health
signal. Coverage denominator: `strategy.strategies` published+enabled = **44** = **38 scalpers** + 6
swing (batch-engine, not live-loaded). The engine's 38 is the full scalper set — §3.10 coverage
complete at the load layer.

## 4 Data-integrity probes

| probe | result |
|---|---|
| §3.15 misaligned (phantom) 1m candles since 09:15 | **0 rows**. Clean, no feed outage. |
| §3.12 futures-OI capture cadence (`NIFTY26JULFUT`, 09:15–11:04) | **65 distinct minutes** over ~109 min. Normal. |
| First-rail histogram (422 rows) | `volume-floor` 370, `time-window` 54, `time-of-day-preference` 10, `option-side-constraint` 6. `volume-floor` dominance = context-bearing rows reaching deep into the gate. |

## 5 Carry into the evening `post` run

1. Confirm the eval ramp held all session (Σ per 3m bar) and 0 fires is regime, not coverage.
2. Confirm `futuresQuadrant = 'NEUTRAL'` share stays low across the full session (§3.12 carry).
3. Re-run the §3.15 phantom-candle probe at EOD — zero at 11:04 only proves no outage yet.
4. Fold this gate's PASS + counter deltas in as the session's opening datapoint.
