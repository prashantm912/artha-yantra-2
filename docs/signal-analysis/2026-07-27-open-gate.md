# Market-open signal-liveness gate — 2026-07-27 (data date)

Analysis date: 2026-07-27, ran 09:36–09:41 IST (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (README §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-27-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint. Ran in the usual ~09:35 window, ~21 min into
the session.

---

## 0 Verdict

**PASS** — the live SignalEngine is receiving *and* evaluating bars. Both liveness gauges read fresh
and track each other across four samples (`ay_signal_bar_received_age_seconds` /
`ay_signal_bar_evaluated_age_seconds`, 28–57 s against a 1m bar interval), Σ
`ay_signal_eval_outcome_total` advanced **+4 over 3m43s**, `ay_signal_eval_failures_total` = 0, and
`strategy.signal_rejections` advanced **12 → 16** during the run (max `generated_at` **09:40:00
IST**). 0 fires — ordinary early-session gating, not a fault.

This run did not need the INCONCLUSIVE branch: rejections were flowing, so step 4's primary read
settled it outright, and the gauges corroborated independently.

## 1 Preconditions

| check | result |
|---|---|
| Host-clock guard (B8) | Host `[DateTime]::UtcNow` = **2026-07-27T04:06:31.5Z**, container `now()` = **04:06:32.1Z**. Drift **<1 s** — no ⚠ CLOCK-DRIFT line; host time service healthy this run. |
| Trading day / in-session | Mon 2026-07-27, **09:36 IST**, inside 09:15–15:30. No `2026-07-27` row in `libs/market-calendar/src/main/resources/nse-trading-holidays.csv`. |
| Stack | 11/11 `ay-*` containers healthy. strategy-signal up ~10 h (boot 2026-07-26 23:20 IST), no restart today. |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-27 09:36:00 IST** (today). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-27T04:06:45Z","tickedTokens":69,"problems":[]}` (04:06Z = 09:36 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **21 bars** each on `NIFTY26JULFUT` / `NIFTY26AUGFUT` / `NIFTY26SEPFUT` at 09:36, **24** by 09:39, max bucket **09:38 IST** (= now−1m). Count tracks minutes-since-open. |
| Eval-stall telemetry | `strategy.subscriber_health_events` since 00:00 IST: **0 rows**. (Absence proves nothing — recorded only because a row present *would* have been FAIL evidence.) |

## 2 THE GATE — step 4 primary: rejections flowing

```sql
SELECT count(*), max(generated_at AT TIME ZONE 'Asia/Kolkata')
  FROM strategy.signal_rejections
 WHERE generated_at >= timestamptz '2026-07-27T09:15:00+05:30';
```

| sample (IST) | rows | max `generated_at` |
|---|---|---|
| 09:36 | 12 | 09:34:00 |
| 09:38 | 14 | 09:37:xx |
| 09:40 | **16** | **09:40:00** |

`>0` with `max` within a few minutes of now ⇒ **PASS** on the primary read. Their presence proves
the gate ran; no absence-based inference was required.

First-rail histogram (at 14 rows): `rsi-band` 6, `time-window` 6, `option-side-constraint` 2.

## 3 Liveness gauges — the definitive read (task_0bed1621)

`docker exec ay-strategy-signal-service sh -c 'wget -qO- http://127.0.0.1:8082/actuator/prometheus' | grep ay_signal_bar_`

| sample (UTC) | `..._received_age_seconds` | `..._evaluated_age_seconds` |
|---|---|---|
| 04:06:45 | 46.071 | 46.068 |
| 04:07:57 | 57.309 | 57.156 |
| 04:10:28 | 28.307 | 27.982 |
| 04:10:36 | 36.778 | 36.453 |

Both gauges **fresh** (≲ 1–2 bar intervals at 1m bars), both **non-negative** (no `-1` "no bar this
boot", no sub-`-1` clock fault), and evaluated-age tracks received-age sample for sample. Per README
§4.3's table that is the *received fresh + evaluated fresh* row ⇒ engine alive. Neither the eval-stall
shape (received fresh, evaluated growing) nor the receive-side stall shape (received growing) is
present.

## 4 Outcome counters (actuator :8082)

| outcome | T1 (04:06:45Z) | T2 (04:07:57Z) | T3 (04:10:28Z) | T4 (04:10:36Z) |
|---|---|---|---|---|
| `chart-gate-failed` | 14 | 14 | 16 | 16 |
| `composite-below-threshold` | 0 | 0 | 0 | 0 |
| `confluence-blocked` | 14 | 14 | 16 | 16 |
| `confluence-gate-absent` | 0 | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 | 0 |
| `unscoreable-indicators-warming` | 0 | 0 | 0 | 0 |
| **Σ** | **28** | **28** | **32** | **32** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 | 0 |

T1→T3 spans **3m43s** (≥ one 3m bar close): Σ **+4**. Counters are in-memory and reset on restart —
only the delta is meaningful. Note the flat T1→T2 segment (72 s, inside one bar): a flat Σ over a
sub-bar window is expected and is *never* on its own an alarm — `ay_signal_eval_outcome_total` is
window- and position-dependent, an attribution primitive rather than a liveness one.

Corroborator — `strategy.signal_eval_outcomes`, **latest bucket only** (never a session-wide `sum()`):

| bucket (IST) | evals |
|---|---|
| 09:33 | 4 |
| 09:30 | 4 |
| 09:27 | 4 |
| 09:24 | 4 |

Fresh bucket, non-zero, landing every 3 min as `SignalEvalOutcomeRollupJob` should ⇒ PASS-QUIET
corroboration on top of the primary read.

## 5 Cold-start strategy load (observation, no action)

```
2026-07-26T17:50:17Z (main) signal engine loaded 38 published strategies (0 dropped on an unresolved universe, 0 failed to load)
```

Boot 2026-07-26 23:20 IST (~10 h uptime, no restart today). **No F10 cold-start shape this time** —
38 loaded on the first pass, no retry needed; `unresolved == 0` is the health signal and it was
satisfied immediately. Coverage denominator: `strategy.strategies` published+enabled = **44** = **38
scalpers** + 6 swing (batch-engine, not live-loaded). The engine's 38 is the full scalper set.

## 6 Data-integrity probes

| probe | result |
|---|---|
| §3.15 misaligned (phantom) 1m candles since 09:15 | **0 rows**. Clean, no feed outage. |
| §3.12 futures-OI capture cadence (`NIFTY26JULFUT`, 09:15–09:38) | **15 distinct OI-bearing minutes** over 24 bars (~62%). In line with 07-24's ~60%. |
| `GET /api/v1/signal-rejections/dot-health` (14 rows scanned, 6 context-bearing) | alive: `breadth`*, `futures_oi`*, `underlying_oi`*, `oi_spurt_price`, `vix`. **dead: `iv_rank`, `dow`, `fii`** — all three `required: false`. (\* = required) |

The three dead dots are optional and match the known dead-dot ledger; 21 min into the session is
early for those inputs to populate. Not a gate failure — carried to the evening run rather than
actioned here.

## 7 Carry into the evening `post` run

1. Confirm the eval ramp held all session (Σ per 3m bar) and 0 fires is regime, not coverage.
2. Re-check `iv_rank` / `dow` / `fii` at EOD — dead at 09:38 only means *not yet populated*; dead at
   15:30 is a real §3 finding against the dead-dot ledger.
3. Re-run the §3.15 phantom-candle probe at EOD — zero at 09:39 only proves no outage yet.
4. Re-check the host-clock guard at EOD; drift was <1 s this morning, but B8 is a free-running-CMOS
   watch item, not a closed one.
5. Fold this gate's PASS + counter deltas in as the session's opening datapoint.
