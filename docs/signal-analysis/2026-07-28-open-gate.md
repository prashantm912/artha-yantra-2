# Market-open signal-liveness gate — 2026-07-28 (data date)

Analysis date: 2026-07-28, ran 09:42–09:54 IST (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (README §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-28-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint. Ran ~27 min into the session.

---

## 0 Verdict

**PASS** — the live SignalEngine is receiving *and* evaluating bars. `strategy.signal_rejections`
advanced **6 → 10** during the run (max `generated_at` **09:52:14 IST**, within ~1 min of wall
clock), so step 4's primary read settled it outright — no absence-based inference was needed. Both
liveness gauges corroborated independently (fresh, non-negative, tracking each other), Σ
`ay_signal_eval_outcome_total` advanced **72 → 144 (+72) over ~3m30s**, and
`ay_signal_eval_failures_total` = 0 on both samples. 0 fires — ordinary early-session gating on a
monthly-expiry tape, not a fault.

## 1 Preconditions

| check | result |
|---|---|
| Host-clock guard (B8) | Host `[DateTime]::UtcNow` = **2026-07-28T04:12:32Z**, container `now()` = **04:12:32.249Z**. Drift **<1 s** — no ⚠ CLOCK-DRIFT line; host time service healthy this run. |
| Trading day / in-session | Tue 2026-07-28, **09:42 IST** at start, inside 09:15–15:30. No `2026-07-28` row in the bundled `libs/market-calendar` holiday resources. |
| Stack | 11/11 `ay-*` containers healthy. strategy-signal up ~7 h (boot 2026-07-27 21:40 UTC = 03:10 IST), no restart during the session. |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-28 09:40:08 IST** (today). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-28T04:18:19Z","tickedTokens":93,"problems":[]}` (04:18Z = 09:48 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **33 bars** each on `NIFTY26JULFUT` / `NIFTY26AUGFUT` / `NIFTY26SEPFUT` at 09:48, max bucket **09:47 IST** (= now−1m); **39** by 09:54. Count tracks minutes-since-open. |
| Eval-stall telemetry | `strategy.subscriber_health_events` since 00:00 IST: **0 rows**. (Absence proves nothing — recorded only because a row present *would* have been FAIL evidence.) |

## 2 THE GATE — step 4 primary: rejections flowing

```sql
SELECT count(*), max(generated_at AT TIME ZONE 'Asia/Kolkata')
  FROM strategy.signal_rejections
 WHERE generated_at >= timestamptz '2026-07-28T09:15:00+05:30';
```

| sample (IST) | rows | max `generated_at` |
|---|---|---|
| 09:49 | 6 | 09:46:14 |
| 09:53 | **10** | **09:52:14** |

`>0` with `max` within a few minutes of now ⇒ **PASS** on the primary read.

First-rail histogram (at 10 rows): `option-side-constraint` 6, `confluence-composite` 2,
`morning-opening-formation` 2.

## 3 Liveness gauges — the definitive read (task_0bed1621)

`docker exec ay-strategy-signal-service sh -c 'wget -qO- http://127.0.0.1:8082/actuator/prometheus' | grep ay_signal_bar_`

| sample (IST) | `..._received_age_seconds` | `..._evaluated_age_seconds` |
|---|---|---|
| 09:48 | 23.543 | 23.534 |
| 09:52 | 42.829 | 28.442 |

Both gauges **fresh** (≲ 1–2 bar intervals at 1m bars), both **non-negative** (no `-1` "no bar this
boot", no sub-`-1` clock fault). Per README §4.3's table this is the *received fresh + evaluated
fresh* row ⇒ engine alive. Neither the eval-stall shape (received fresh, evaluated growing) nor the
receive-side stall shape (received growing) is present. Evaluated-age reading *younger* than
received-age at 09:52 is not a defect — the two stamps are independent per-bar marks and both sit
well inside one bar interval.

## 4 Outcome counters (actuator :8082)

| outcome | T1 (09:48 IST) | T2 (09:52 IST) | Δ |
|---|---|---|---|
| `chart-gate-failed` | 36 | 72 | +36 |
| `composite-below-threshold` | 30 | 62 | +32 |
| `confluence-blocked` | 6 | 10 | +4 |
| `confluence-gate-absent` | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 |
| `unscoreable-indicators-warming` | 0 | 0 | 0 |
| **Σ** | **72** | **144** | **+72** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 |

T1→T2 spans ~3m30s (≥ one 3m bar close): Σ **+72**. Counters are in-memory and reset on restart —
only the delta is meaningful. A session that is all `chart-gate-failed` + `composite-below-threshold`
is a quiet market, not a fault; `confluence-blocked` (+4) is the only outcome that also writes a
`signal_rejections` row, which matches the 6 → 10 row advance exactly.

Corroborator — `strategy.signal_eval_outcomes`, **latest bucket only** (never a session-wide `sum()`):

| bucket (IST) | evals |
|---|---|
| 09:45 | 36 |
| 09:42 | 4 |
| 09:39 | 4 |

Fresh bucket, non-zero, landing every 3 min as `SignalEvalOutcomeRollupJob` should ⇒ PASS-QUIET
corroboration on top of the primary read. The 09:45 jump (4 → 36) is the eval fan-out widening as
more strategies entered their session windows, consistent with the counter delta above.

## 5 Cold-start strategy load (observation, no action)

```
2026-07-27T21:40:45Z (main) signal engine loaded 38 published strategies (0 dropped on an unresolved universe, 0 failed to load)
```

Boot 2026-07-27 21:40 UTC = 03:10 IST (~7 h uptime, no restart today). **No F10 cold-start shape** —
38 loaded on the first pass; `unresolved == 0` is the health signal and it was satisfied
immediately. Coverage denominator: `strategy.strategies` published+enabled = **44** = **38
scalpers** + 6 swing (batch-engine, not live-loaded). Unchanged from 07-27.

## 6 Data-integrity probes

| probe | result |
|---|---|
| §3.15 misaligned (phantom) 1m candles since 09:15 | **0 rows**. Clean, no feed outage. |
| §3.12 futures-OI capture cadence (09:15–09:54) | `NIFTY26AUGFUT` **37 / 39** bars carry OI (~95%). `NIFTY26JULFUT` **0 / 39** — see below. |
| `GET /api/v1/signal-rejections/dot-health` (10 rows scanned, 4 context-bearing) | alive: `breadth`*, `fii`, `vix`. dead: `iv_rank`, `dow`, `oi_spurt_price` (all `required: false`). `futures_oi` / `underlying_oi` report **"NEUTRAL by design — monthly index-expiry day, OI reads S24-suppressed (not an outage)"**. |

`* = required`

**JUL-future OI is a roll artifact, not an outage.** July OI was already decaying yesterday
(07-27: `NIFTY26JULFUT` 16 / 375 bars vs `NIFTY26AUGFUT` 364 / 375) and reads 0 / 39 today on
monthly expiry. The live OI-bearing front is AUG at ~95% cadence — better than 07-27's ~60% figure
on the then-front. No capture problem.

`iv_rank` / `dow` dead at 09:54 matches the known dead-dot ledger and is early-session normal;
`oi_spurt_price` newly reads dead this morning (it was alive at 07-27 09:38) — plausibly the same
expiry-day OI suppression, but that is a hypothesis, not a verified cause.

## 7 Carry into the evening `post` run

1. Confirm the eval ramp held all session (Σ per 3m bar) and 0 fires is regime, not coverage —
   today is a monthly index-expiry Tuesday, so expect atypical gating.
2. Re-check `iv_rank` / `dow` / `oi_spurt_price` at EOD — dead at 09:54 only means *not yet
   populated*; dead at 15:30 is a real §3 finding. Specifically test whether `oi_spurt_price` going
   dead is expiry-day OI suppression or a genuine new dead dot.
3. Re-check `futures_oi` / `underlying_oi` on the NEXT non-expiry session — the S24 "NEUTRAL by
   design" reading should clear on its own; if it persists, it is a finding.
4. Re-run the §3.15 phantom-candle probe at EOD — zero at 09:54 only proves no outage yet.
5. Re-check the host-clock guard at EOD; drift was <1 s this morning, but B8 is a free-running-CMOS
   watch item, not a closed one.
6. Fold this gate's PASS + counter deltas in as the session's opening datapoint.
