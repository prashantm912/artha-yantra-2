# Market-open signal-liveness gate — 2026-07-29 (data date)

Analysis date: 2026-07-29. Gate window **09:36–09:42 IST**; supplementary probes + one midday
corroboration sample at **12:43 IST** (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (README §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-29-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint. The gate settled at 09:42 IST, ~27 min into
the session; the run then stayed open long enough to add a 12:43 IST sample, which is reported
separately rather than blended into the gate readings.

---

## 0 Verdict

**PASS** — the live SignalEngine is receiving *and* evaluating bars. `strategy.signal_rejections`
advanced **14 → 16** during the gate window (max `generated_at` **09:40:00 IST**, within ~2 min of
wall clock), so README §4.3 step 4's primary read settled it outright — no absence-based inference
was needed. Both liveness gauges corroborated independently (fresh, non-negative, tracking each
other), Σ `ay_signal_eval_outcome_total` advanced **28 → 32 over ~4 min**, and
`ay_signal_eval_failures_total` = 0 on every sample. The midday sample re-confirms the same picture
at scale (Σ **2100**, 854 rejections, **6 fires**).

## 1 Preconditions

| check | result |
|---|---|
| Host-clock guard (B8) | Host `[DateTime]::UtcNow` = **2026-07-29T04:06:44Z**, container `now()` = **04:06:45.04Z**. Drift **~1 s** — no ⚠ CLOCK-DRIFT line; host time service healthy this run. Re-checked at 07:13Z: host, `ay-timescaledb`, `ay-strategy-signal-service` and `ay-market-data-service` all read **07:13:04–05Z** (all four agree). |
| Trading day / in-session | Wed 2026-07-29, **09:36 IST** at start, inside 09:15–15:30. No `2026-07` row at all in the bundled `libs/market-calendar/src/main/resources/nse-trading-holidays.csv`. Non-expiry day (NSE weekly = Tuesday). |
| Stack | 11/11 `ay-*` containers healthy. strategy-signal up ~8 h (boot **2026-07-28 20:25:31Z** = 01:55 IST), no restart during the session. |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-29 09:34:51 IST** (today). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-29T04:07:03Z","tickedTokens":105,"problems":[]}` (04:07Z = 09:37 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **22 bars** each on `NIFTY26AUGFUT` / `NIFTY26SEPFUT` / `NIFTY26OCTFUT` at 09:37, max bucket **09:36 IST** (= now−1m). Count tracks minutes-since-open; **209** bars by 12:43. |
| Eval-stall telemetry | `strategy.subscriber_health_events` since 00:00 IST: **0 rows**. (Absence proves nothing — recorded only because a row present *would* have been FAIL evidence.) |

## 2 THE GATE — step 4 primary: rejections flowing

```sql
SELECT count(*), max(generated_at AT TIME ZONE 'Asia/Kolkata')
  FROM strategy.signal_rejections
 WHERE generated_at >= timestamptz '2026-07-29T09:15:00+05:30';
```

| sample (IST) | rows | max `generated_at` |
|---|---|---|
| 09:37 | 14 | 09:37:00 |
| 09:42 | **16** | **09:40:00** |
| 12:43 (corroboration) | 854 | 12:43:17 |

`>0` with `max` within a few minutes of now ⇒ **PASS** on the primary read. No INCONCLUSIVE branch
was entered.

First-rail histogram at 16 rows (09:42): `time-window` 6, `rsi-band` 4, `volume-floor` 2,
`option-side-constraint` 2. At 854 rows (12:43) the population is dominated by `volume-floor` 643 /
`time-window` 102, then `two-candle` 12, `volume-pump` 12, `rsi-band` 12, `pct-price-move` 12,
`divergence-vol-gate` 10, `time-of-day-preference` 10, `oi-cross-required` 8,
`directional-change-gate` 8, `max-oi-sr-gate` 7, `call-put-delta-filter` 4.

## 3 Liveness gauges — the definitive read (task_0bed1621)

`docker exec ay-strategy-signal-service sh -c 'wget -qO- http://127.0.0.1:8082/actuator/prometheus' | grep ay_signal_bar_`

| sample (IST) | `..._received_age_seconds` | `..._evaluated_age_seconds` |
|---|---|---|
| 09:37 | 13.225 | 13.054 |
| 09:38 | 59.135 | 58.964 |
| 09:42 | 3.066 | 3.064 |
| 12:43 (corroboration) | 22.936 | 6.048 |

Both gauges **fresh** (≲ 1–2 bar intervals at 1m bars), both **non-negative** on every sample (no
`-1` "no bar this boot", no sub-`-1` clock fault). Per README §4.3's table this is the *received
fresh + evaluated fresh* row ⇒ engine alive. Neither stall shape is present: received-age never grew
while capture was healthy, and evaluated-age never grew away from received-age.

## 4 Outcome counters (actuator :8082)

| outcome | T1 (09:37 IST) | T2 (09:42 IST) | Δ | T3 (12:43 IST) |
|---|---|---|---|---|
| `chart-gate-failed` | 14 | 16 | +2 | 1178 |
| `composite-below-threshold` | 0 | 0 | 0 | 62 |
| `confluence-blocked` | 14 | 16 | +2 | 854 |
| `confluence-gate-absent` | 0 | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 | **6** |
| `unscoreable-indicators-warming` | 0 | 0 | 0 | 0 |
| **Σ** | **28** | **32** | **+4** | **2100** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 | 0 |

T1→T2 spans ~4 min (≥ one 3m bar close): Σ **+4**, i.e. ADVANCING ⇒ PASS. Counters are in-memory and
reset on restart — only the delta is meaningful. `confluence-blocked` is the only outcome that also
writes a `signal_rejections` row, and its 14 → 16 advance matches the row advance exactly.

Corroborator — `strategy.signal_eval_outcomes`, **latest bucket only** (never a session-wide `sum()`):

| bucket (IST) | evals |
|---|---|
| 09:39 | 4 |
| 09:36 | 4 |
| 12:39 (corroboration) | 32 |
| 12:36 | 32 |

Fresh bucket, non-zero, landing every 3 min as `SignalEvalOutcomeRollupJob` should ⇒ PASS-QUIET
corroboration on top of the primary read. The 4 → 32 widening between the open window and midday is
the eval fan-out as more strategies entered their session windows.

## 5 Cold-start strategy load (observation, no action)

```
2026-07-28T20:25:31Z (main) signal engine loaded 38 published strategies (0 dropped on an unresolved universe, 0 failed to load)
```

Boot 2026-07-28 20:25 UTC = 01:55 IST (~8 h uptime, no restart today). **No F10 cold-start shape** —
38 loaded on the first pass; `unresolved == 0` is the health signal and it was satisfied
immediately. Coverage denominator: `strategy.strategies` published+enabled = **44** = **38
scalpers** + 6 swing (batch-engine, not live-loaded). Unchanged from 07-27 / 07-28.

## 6 Data-integrity probes

| probe | result |
|---|---|
| §3.12 futures-OI capture cadence (09:15–12:43) | `NIFTY26AUGFUT` **209 / 209** bars carry OI (**100 %**). `NIFTY26SEPFUT` 0 / 208 — normal, OI rides the front month. |
| `GET /api/v1/signal-rejections/dot-health` @ 12:42 IST (200 rows scanned, 40 context-bearing) | alive: `breadth`*, `fii`, `oi_spurt_price`, `vix`, `futures_oi`*, `underlying_oi`*. dead: `iv_rank`, `dow` (both `required: false`). |

`* = required`

**Yesterday's carry items #2 and #3 are CLEARED.** `futures_oi` / `underlying_oi` read live again
today — 07-28's "NEUTRAL by design — monthly index-expiry day, S24-suppressed" reading cleared on
its own on the first non-expiry session, exactly as [[monthly-expiry-oi-suppression]] predicts, so
it was suppression and not an outage. `oi_spurt_price`, dead at 07-28 09:54, is **alive** today —
consistent with the same expiry-day-suppression hypothesis rather than a new dead dot. `iv_rank` and
`dow` remain dead and stay on the known dead-dot ledger.

Front-month OI cadence is also materially better than 07-28 (100 % vs ~95 %), which is the roll
completing: AUG is now the sole OI-bearing front.

## 7 Fires and execution (new vs 07-28 — the tape was not quiet today)

`strategy.signals` since 09:15 IST (all intraday-stamped, so live engine — not the swing batch,
whose rows stamp `00:00:00`): **10 rows**, composite **0.8569** (11:06 / 11:36 / 11:39) and
**0.8429** (12:12 / 12:18), all `book='scalper'`. Legs: `NIFTY2680424000CE`,
`SENSEX26JUL77200CE` (BUY, `EXPIRED`), plus SELL rows with an empty `tradeable_tradingsymbol`
(`ACTIVE`). `strategy.paper_positions` opened today: **2** (`scalper`, 11:07:20 and 12:13:17 IST).

⚠️ **Open discrepancy for the evening run, NOT explained here:** the `fired` counter reads **6**
while `strategy.signals` holds **10** intraday rows for the same window, and only **2** paper
positions opened. Both numbers are *sourced* (counter = actuator, rows = SQL); the mapping between
an eval-level `fired` outcome, an emitted signal row and an opened position is *not* verified in
this run — do not assume a defect. The 3 `EXPIRED` BUY rows at 11:06 and the empty-symbol SELL rows
are the obvious places to start.

## 8 Carry into the evening `post` run

1. Resolve §7's `fired` 6 vs 10 signal rows vs 2 paper positions. Check `emitted_at` /
   `emit_latency_ms` / `status` transitions and whether the empty `tradeable_tradingsymbol` SELL
   rows are exit signals rather than entries.
2. `volume-floor` at **643 / 854** rejections (75 %) is by far the dominant first rail — check
   whether that is the armed relative-volume floor (k=1.5 / N=20 / minBars=10) binding structurally
   or an ordinary low-volume midday tape. Single-session, so classify STRUCTURAL vs REGIME before
   proposing anything.
3. Re-check `iv_rank` / `dow` at EOD — dead at 12:42 is not yet an EOD finding.
4. Confirm the 100 % AUG OI cadence held all session.
5. Re-check the host-clock guard at EOD; drift was ~1 s this morning, but B8 is a free-running-CMOS
   watch item, not a closed one.
6. Fold this gate's PASS + counter deltas in as the session's opening datapoint.
