# Market-open signal-liveness gate — 2026-07-30 (data date)

Analysis date: 2026-07-30. Gate window **09:36–09:58 IST** (scheduled `session-analysis open`).
Analyst: Claude (scheduled market-open gate). **Read-only run** — no restarts, no writes, no deploys.
Verdict: **PASS.**

Scope note: this is the *open gate only* (README §4.3 starvation check), not the §3 forensic pass.
The evening `session-analysis post` run owns `2026-07-30-session-findings.md` and should fold this
file's numbers in as the session's opening datapoint.

---

## 0 Verdict

**PASS** — the live SignalEngine is receiving *and* evaluating bars. README §4.3 step 4's primary
read settled it outright: `strategy.signal_rejections` advanced **14 → 32** during the window (max
`generated_at` **09:46:24 IST**), so no absence-based inference was needed. Both liveness gauges
corroborated on all four samples (fresh, non-negative, tracking each other), Σ
`ay_signal_eval_outcome_total` advanced **24 → 28 → 32 → 216**, and `ay_signal_eval_failures_total`
= 0 throughout.

## 1 Preconditions

| check | result |
|---|---|
| Host-clock guard (B8) | Host `[DateTime]::UtcNow` = **2026-07-30T04:06:34Z**, container `now()` = **04:06:34.917Z**. Drift **0.1 s** — no ⚠ CLOCK-DRIFT line; host time service healthy this run. |
| Trading day / in-session | Thu 2026-07-30, **09:36 IST** at start, inside 09:15–15:30. No `2026-07` row at all in the bundled `libs/market-calendar/src/main/resources/nse-trading-holidays.csv`. |
| Expiry context | ⚠️ **BSE MONTHLY expiry** (last Thursday of July). Per [[monthly-expiry-oi-suppression]] the OI block is suppressed **per root**, so SENSEX-rooted scalpers run with an inert OI bloc **by design** today; NIFTY-rooted ones are unaffected (NSE monthly was 07-28). Do not read a SENSEX-side OI NEUTRAL today as a fault. |
| Stack | 11/11 `ay-*` containers healthy, all up ~2 h. strategy-signal boot **2026-07-30T02:36:13Z** = 08:06 IST — pre-open, no restart during the session. |
| Kite login | `marketdata.kite_session.last_validated_at` = **2026-07-30 09:36:13 IST** (today). |
| market-data canary | `GET /api/v1/market/health/data` → `{"status":"GREEN","marketOpen":true,"asOf":"2026-07-30T04:06:46Z","tickedTokens":69,"problems":[]}` (04:06Z = 09:36 IST). |
| Capture fresh | `marketdata.candles` NFO 1m since 09:15: **21 bars** each on `NIFTY26AUGFUT` / `NIFTY26SEPFUT` / `NIFTY26OCTFUT` at 09:37, max bucket **09:35 IST** (= now−1m). Count tracks minutes-since-open; **42** bars by 09:58. |
| Eval-stall telemetry | `strategy.subscriber_health_events` since 00:00 IST: **0 rows**. (Absence proves nothing — recorded only because a row present *would* have been FAIL evidence.) |
| Build fingerprint (§3.23) | Both `ay_signal_bar_*_age_seconds` gauges are **served** by the running container, which is itself proof the deployed jar carries task_0bed1621. Actuator on **:8082** as expected. |

## 2 THE GATE — step 4 primary: rejections flowing

```sql
SELECT count(*), max(generated_at AT TIME ZONE 'Asia/Kolkata')
  FROM strategy.signal_rejections
 WHERE generated_at >= timestamptz '2026-07-30T09:15:00+05:30';
```

| sample (IST) | rows | max `generated_at` |
|---|---|---|
| 09:37 | 14 | 09:37:01 |
| 09:41 | 14 | 09:37:01 |
| 09:58 | **32** | **09:46:24** |

`>0` with rows advancing ⇒ **PASS** on the primary read. No INCONCLUSIVE branch was entered. Note
the 09:41 sample was flat against 09:37 — that is the ordinary shape README §4.3 warns about
(`confluence-blocked` is the only outcome that writes a row), and the gauges plus the outcome
counters were what carried the read in that interval.

First-rail histogram at 32 rows (09:58): `volume-floor` 12, `time-window` 10,
`morning-eod-precondition` 4, `option-side-constraint` 2, `time-of-day-preference` 2, `rsi-band` 2.

## 3 Liveness gauges — the definitive read (task_0bed1621)

`docker exec ay-strategy-signal-service sh -c 'wget -qO- http://127.0.0.1:8082/actuator/prometheus' | grep ay_signal_bar_`

| sample (IST) | `..._received_age_seconds` | `..._evaluated_age_seconds` |
|---|---|---|
| 09:37:00 | 0.478 | 60.454 |
| 09:37:58 | 57.754 | 56.803 |
| 09:41:45 | 45.055 | 45.052 |
| 09:58:14 | 14.654 | 12.509 |

Both gauges **fresh** and **non-negative** on every sample (no `-1` "no bar this boot", no sub-`-1`
clock fault) ⇒ README §4.3's *received fresh + evaluated fresh* row = engine alive. Neither stall
shape is present: received-age never grew while capture was healthy, and evaluated-age never grew
away from received-age. The 09:37:00 pair (received 0.5 s / evaluated 60 s) is the expected
1m-receipt vs 3m-primary-evaluation offset, not a lag — the 09:36 3m bar had just been evaluated.

## 4 Outcome counters (actuator :8082)

| outcome | T1 09:37:00 | T2 09:37:58 | T3 09:41:45 | T4 09:58:14 |
|---|---|---|---|---|
| `chart-gate-failed` | 12 | 14 | 16 | 118 |
| `composite-below-threshold` | 0 | 0 | 2 | 66 |
| `confluence-blocked` | 12 | 14 | 14 | 32 |
| `confluence-gate-absent` | 0 | 0 | 0 | 0 |
| `discipline-paused` | 0 | 0 | 0 | 0 |
| `fired` | 0 | 0 | 0 | 0 |
| `unscoreable-indicators-warming` | 0 | 0 | 0 | 0 |
| **Σ** | **24** | **28** | **32** | **216** |
| `ay_signal_eval_failures_total` | 0 | 0 | 0 | 0 |

Σ ADVANCING on every interval ⇒ PASS. Counters are in-memory and reset on restart — only the delta
is meaningful. `confluence-blocked` (the only outcome that also writes a `signal_rejections` row)
matches the row counts exactly at every sample (12/14/14/32 vs 14/14/32 — the T1 counter read
preceded the row insert by ~1 s).

Corroborator — `strategy.signal_eval_outcomes`, **latest bucket only** (never a session-wide `sum()`):

| bucket (IST) | evals |
|---|---|
| 09:27 | 4 |
| 09:30 | 4 |
| 09:33 | 4 |
| 09:48 | 36 |
| 09:51 | 36 |
| 09:54 | 36 |

Fresh, non-zero, landing every 3 min as `SignalEvalOutcomeRollupJob` should. The **4 → 36** step is
the documented 09:45 in-window fan-out (README §4.3's "before ~09:45 only a handful of strategies are
in-window"), reproduced here for a third session — pre-09:45 flatness is the trade WINDOW, not a stall.

## 5 Cold-start strategy load (observation, no action needed)

```
2026-07-30T02:36:13Z (main)        signal engine loaded 0 published strategies (38 dropped on an unresolved universe, 0 failed to load)
2026-07-30T02:37:38Z (signal-eval) signal engine loaded 38 published strategies (0 dropped on an unresolved universe, 0 failed to load)
```

**F10 cold-start shape present, and it self-healed in 85 s** — the first pass at boot dropped all 38
on an unresolved universe; the `signal-eval` re-resolve at 02:37:38Z brought `unresolved` to **0**
with 38 loaded. Per [[live-mode-findings]] the health signal is `unresolved == 0`, never `loaded > 0`,
and it was satisfied ~1.5 min after boot and ~97 min before the open. Nothing to act on; recorded
because the shape is the F10 Part-B watch item.

Coverage denominator: `strategy.strategies` published+enabled = **44** = **38 scalpers** (live-loaded)
+ 6 swing (batch engine, not live-loaded). Unchanged from 07-27 / 07-28 / 07-29.

## 6 Data-integrity probes

| probe | result |
|---|---|
| §3.12 futures-OI capture cadence (09:15–09:58) | `NIFTY26AUGFUT` **41 / 42** bars carry OI (~98 %). |
| `GET /api/v1/signal-rejections/dot-health` @ 09:37:44 IST (14 rows scanned, 6 context-bearing) | alive: `breadth`\*, `fii`, `oi_spurt_price`, `vix`, `iv_abs_band`, `futures_oi`\*, `underlying_oi`\*. dead: `iv_rank`, `dow` (both `required: false`). |

`* = required`

**No change vs 07-29.** Dead set is exactly `{iv_rank, dow}` — the standing state (`iv_rank`
dead-data carried since 07-02, `dow` NULL by design / un-armed), both `required: false`. `fii` reads
alive, same as 07-29 12:42 — that is **not** news against the 07-29 baseline (it is only news against
the older 07-24 ledger row, which 07-29 already superseded). Per README §4.1, only a *change* in that
set is reportable. ⚠️ Support rates are NOT reported here — a mid-session dot read is PROVISIONAL
(§3.21); the EOD run owns them.

## 7 Fires and execution

`fired` counter **0** and `strategy.signals` intraday rows since 09:15 IST **0** at 09:58 — the two
agree. No paper entry attempted in the first 43 min. All blocks split
`chart-gate-failed` 118 / `composite-below-threshold` 66 / `confluence-blocked` 32, i.e. a gated tape,
not a fault. No `discipline-paused`, no `unscoreable-indicators-warming`, no eval failures.

## 8 Carry into the evening `post` run

1. `volume-floor` at **12 / 32** first-rail blocks is the largest single rail again (75 % at 07-29
   midday). Classify STRUCTURAL vs REGIME before anything is proposed — and remember the standing
   prior: **all four measured loosenings of the entry gate lost money** (T1, T7, G13, G10).
2. `tickedTokens` = **69** this morning vs **105** on 07-29. Plausibly the SENSEX weekly/monthly
   contract set rolling off on today's BSE monthly expiry, but that is *assumed*, not verified —
   confirm at EOD before it becomes a carried fact.
3. Today is the **BSE monthly expiry**, so any SENSEX-root OI NEUTRAL is S24 suppression by design.
   Do not open a dead-dot item off it; re-check on the next non-expiry session.
4. Confirm the ~98 % AUG OI cadence held all session (1 missing bar at 09:58 is the in-progress
   bucket, most likely).
5. Re-check `iv_rank` / `dow` at EOD — dead at 09:37 is not yet an EOD finding.
6. Re-check the host-clock guard at EOD; drift was 0.1 s this morning, but B8 is a
   free-running-CMOS watch item, not a closed one.
7. Stamp the §Session regime row (G15) from the `NIFTY 50` daily bar using **intraday** open→close,
   and if the label is `chop`, say so and re-read ledger row G11.
8. Fold this gate's PASS + counter deltas in as the session's opening datapoint.
