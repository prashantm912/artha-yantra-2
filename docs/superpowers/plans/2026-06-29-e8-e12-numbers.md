# E8/E9/E11/E12 owner-number build — plan + owner answers (2026-06-29)

**Status (2026-06-30): PARTIALLY SHIPPED — still ACTIVE.** E8 armed (#349) + E9 fixed-% take-profit (#351)
landed; E9-remaining (vol-qualified VWAP exit, points/SuperTrend-level trails), E11 (D21 BTST 50%, Sensex
3.0× scale, direction-both resolver, straddle combined-prem) and E12 (ideal-window, backtest rails) are still
OPEN epics in the scalper-to-100 roadmap. Kept active — it carries the load-bearing owner answers + the
doc-specified numbers feeding those open epics.

Owner: "start building E8/E9/E11/E12 numbers one by one autonomously, ask me questions in advance together."
Investigation: workflow `wf_ae3bb32f-765` (4 readers, one per epic) separated doc-specified numbers from genuine
owner-decisions. Build recipe per package = the established tag-gate pattern (`cfg.has("tag")` default-OFF →
arm on the doc rule→strategy home → `ScalperStrategyLoadTest` "armed iff <family>" tripwire; parity-safe by the
scalper-seam firewall, engine goldens byte-identical).

## OWNER ANSWERS (batched AskUserQuestion, 2026-06-29)
1. **E9 take-profit design = BOTH, as tunable knobs.** Build a fixed premium-% target AND a PSAR→SuperTrend
   trail as default-OFF exit_rules; arm TARGET-based on gap-theory/market-movers, TRAIL-based on the rest, per
   the doc map. Default bands (~35% target, +15% activate / 10% trail) DB/optimizer-tunable.
2. **E11 Sensex point-scale = 3.0×** (tunable `SENSEX_POINT_SCALE` constant; conservative end of the doc's 3-4×;
   refine from live ratio later if data supports).
3. **E12 afternoon entry cap = NONE** (no fresh-entry cap; rely on the existing 15:12 square-off only). → do NOT
   build the afternoon-cap gate.

## DOC-SPECIFIED numbers (use directly, NO owner question)
- E11 **D21 BTST overnight SL = 50% of premium** on the carried leg (operative §3.8 L515 + BTST digest row 1630).
- E9 **gap-fill deadline = 40 min** (already built #332).
- E8 **time-of-day window = 10:00–13:30** (PREFER constants), **vwap-distance 0.4%** max-frac (ScalperGates const).
- E12 Hero-Zero entry floor stays **14:30** (configurable; no S24 evidence for 14:45).

## KEEP-MANUAL / DEFERRED (no build)
- E9 OI-confirmed SL leeway (10-20pt benefit-of-doubt) — stays MANUAL to keep the persisted stop deterministic.
- E12 event lockout — CLOSED as permanent-manual (#339, no feed; owner uses `news_clear`).
- PE-mirror seeding — owner SKIPPED earlier (directional scalpers stay CE-only).

## STATUS + PER-EPIC RECIPE
### E8 — DONE (#349)
Gates were already built+wired (timeOfDayPreference/vwapDistance/priorDayVwapStop + always-on graded
`sizeMultiplier`). Armed the 2 doc-explicit homes: **time-of-day-preference → trending-oi** (§3.5 window),
**vwap-distance → gap-theory** (§3.4 pullback). Tripwire updated. Owner may later extend vwap-distance to
two-candle/connect-dots + add the hero-zero VWAP-pin sit-out (needs a per-strategy `min-frac` param).

### E9 — trade-management (the heaviest; parity-sensitive — changes scalper backtest EXIT behavior)
1. **target/trail** (owner: both): add `take_profit{basis: premium_pct, value: ~35}` + `trailing_stop{basis:
   premium_pct, value: ~10, activate: ~15}` exit_rules to gap-theory + market-movers (TARGET families);
   `trailing_stop` only on the TRAIL families (trend-change/golden-crossover/two-candle/etc.). FIRST verify the
   engine `trailing_stop` semantics (does premium_pct support an activate-threshold + trail-%, or trail-from-peak
   only?) — and how scalper exits interact (square_off + signal_exit + structural stops already manage exits, so
   avoid a redundant/contradictory take_profit). Each YAML armed → tripwire "target-trail armed iff <family>".
2. **vol-qualified VWAP exit** — reuse the entry volume floor as the exit's qualifier (one source of truth).
3. **points trail** — `trailing_stop{basis: index_points}` with the per-index band (Nifty 30-60 etc.).
4. **SuperTrend-level stop** — NEW `ScalperConfig.StructuralStop.SUPERTREND_LEVEL` enum + ExitEvaluator wiring
   off the SUPERTREND_LINE indicator (built #274 W0). Real engine work; gate behind `supertrend-level-stop`.
5. **D4 confluence-exit** = `oi-confluence-exit` — already armed on 6 YAMLs (DONE #331).
6. gap-fill deadline — DONE #332.

### E11 — per-strategy controls
1. **D21 50%-premium BTST stop** — a premium-% stop on the carried leg (50% of entry premium → exit). Live-managed
   on the BTST carry path; check whether it's a tag-gated check in the carry route (preCloseEvaluate / the carry
   monitor). Armed on scalp-btst-stbt.
2. **direction:both→side resolver** — recommended: close-vs-day-high/low (BTST pre-close bar): close ≥ upper →
   CE/LONG, ≤ lower → PE/SHORT. (The #330 forcedSide overload already does close-upper/lower-half — verify if this
   is already covered; may be DONE.)
3. **Sensex 3.0× point-scale** — `SENSEX_POINT_SCALE = 3.0` constant; multiply the NIFTY-signal point stops/targets
   by 3.0 when the option-root is SENSEX (the point-SL #336 is in NIFTY-signal pts; SENSEX needs the ×3 widen).
4. **straddle combined-prem** — combined two-leg premium-vs-VWAP exit (StraddleChartService is display-only; the
   live gate is #324 MANUAL — confirm scope).

### E12 — time gates + backtest rails
1. afternoon cap — owner said NONE → skip.
2. **ideal-window** (09:15–10:00 fresh-entry preference) — optional default-OFF `ideal-window` hard-skip tag; arm
   only where early entries matter. Low priority.
3. Hero-Zero floor — keep 14:30 (configurable property). No change.
4. **backtest RSI/volume rails for `direction: both`** — parity-faithfulness: the live seam re-applies RSI+volume
   rails on the BTST path; the backtest gate.all must match. Recommended: skip RSI for `direction: both` (conservative).
   Parity-sensitive — verify against BacktestParityTest.

## COMPLETION (2026-06-29) — investigation found most was ALREADY BUILT; only 2 genuine gaps filled
Verifying each before building (the E8 lesson) showed E8/E9/E11/E12 were largely already implemented:
- **E8 — DONE (#349):** gates built; armed time-of-day-preference→trending-oi + vwap-distance→gap-theory.
- **E9 — DONE (#351 + already built):** the directional scalpers ALREADY had the vol-qualified VWAP `signal_exit`
  (min_volume), the SuperTrend-level `trailing_stop` (basis: indicator / `supertrend_line` — NO new enum needed),
  `time_stop`, the D4 `oi-confluence-exit` (#331), gap-fill (#332). The ONLY gap = the fixed premium-% TARGET →
  added `take_profit{premium_pct: 35}` to gap-theory + market-movers (#351). points-trail = redundant (ST-trail
  already covers trailing).
- **E11 — DONE (already built / descoped):** D21 50%-premium BTST stop ALREADY on all 3 btst YAMLs
  (`stop_loss{premium_pct: 50}` + `time_stop` 1-night); direction:both→side resolver ALREADY done (#330
  close-vs-day-high/low at the pre-close gate); **Sensex point-scale = DESCOPED** (all 2b scalpers signal on
  NIFTY-FUT-CONT so stops are uniformly in NIFTY-signal points — the 60-pt SL already ≈ 250 SENSEX-native; a
  `SENSEX_POINT_SCALE` constant would be dead code); straddle combined-prem = #324 (manual); PE-mirror = owner-skipped.
- **E12 — no meaningful build:** afternoon cap = owner-NO; backtest RSI-rails for `direction: both` = a
  backtest-FIDELITY nicety (low value — backtests are functional-only, owner judges live); ideal-window
  (09:15-10:00) = an OPTIONAL default-OFF gate, unbuilt, low value (soft doc preference). Left for the owner.

**NET: the meaningful E8/E9/E11/E12 owner-number builds are COMPLETE** (#349 + #351); the rest was already built,
descoped, owner-declined, or low-value optional. The genuine remaining frontier is feed-blocked (E3 Dow
`/global-cues`), v2 (E1 full-N50 + short side), and the LIVE run (the only real validation).
