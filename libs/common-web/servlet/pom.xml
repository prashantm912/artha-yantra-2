<?xml version="1.0" encoding="UTF-8"?>
<!-- common-web-servlet (A.3): the servlet adapter — GlobalExceptionHandler
     mapping framework exceptions to the D8 envelope + the gateway-identity
     header filter. Never on the reactive gateway's classpath (A.2.4). -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>in.arthayantra</groupId>
    <artifactId>common-web</artifactId>
    <version>2.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>common-web-servlet</artifactId>
  <name>common-web — servlet adapter</name>

  <dependencies>
    <dependency>
      <groupId>in.arthayantra</groupId>
      <artifactId>common-web-core</artifactId>
      <version>${project.version}</version>
    </dependency>
    <!-- provided: every consumer is itself a webmvc service bringing its own
         starter-web; this library must not impose the version -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
      <scope>provided</scope>
    </dependency>
    <!-- provided, same reasoning: the @Validated method-validation mapping names
         ConstraintViolationException, and all three consumers already carry the API at
         compile scope (springdoc-openapi-starter-common → spring-boot-starter-validation) -->
    <dependency>
      <groupId>jakarta.validation</groupId>
      <artifactId>jakarta.validation-api</artifactId>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
