<?xml version="1.0" encoding="UTF-8"?>
<!-- strategy-schema (Phase 18 / ADR D18): strategy-schema/v1 JSON Schema +
     YAML loading (SnakeYAML SafeConstructor, 256 KB cap), canonicalization,
     SHA-256 checksums and the semantic validation the schema cannot express.
     Pure library - no Spring; consumed by strategy-signal-service and
     backtest-service. The schema document FREEZES at the Stage-C exit gate. -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>in.arthayantra</groupId>
    <artifactId>arthayantra-parent</artifactId>
    <version>2.0.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>strategy-schema</artifactId>
  <name>strategy-schema</name>

  <properties>
    <!-- CD-4: networknt draft 2020-12 validator -->
    <json-schema-validator.version>1.5.6</json-schema-validator.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
    </dependency>
    <dependency>
      <groupId>com.networknt</groupId>
      <artifactId>json-schema-validator</artifactId>
      <version>${json-schema-validator.version}</version>
    </dependency>

    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
