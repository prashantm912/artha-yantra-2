# expect: /risk/position_sizing
schema: strategy-schema/v1
id: reject-kelly-over-cap
name: "Kelly Over Cap"
version: 1.0.0
universe:
  mode: explicit
  instruments:
    - { exchange: NSE, tradingsymbol: RELIANCE }
timeframes: { primary: 1d }
indicators:
  - { name: EMA, alias: ema20, timeframe: 1d, params: { period: 20 }, weight: 1.0 }
entry_rules:
  direction: long
  gate:
    all:
      - "close > ema20"
  scoring: { threshold: 0.5 }
exit_rules:
  - { type: stop_loss, params: { basis: r_multiple, value: 1.5 } }
risk:
  position_sizing: { method: kelly_fraction, params: { fraction: 0.5 } }   # HARD-CAPPED at 0.25
  max_positions: 1
  session: { style: positional, allow_overnight: true }
