# Runtime deps (CD-15: plain pip + ruff). Hand-edited source of truth — pinned exact
# versions. After ANY change here, regenerate the hash-locked install set:
#   uv pip compile --native-tls --generate-hashes --python-version 3.12 \
#     -o requirements.lock requirements.txt
# The Dockerfile installs requirements.lock under --require-hashes (supply-chain guard);
# CI installs requirements-dev.lock (regen the same way from requirements-dev.txt).
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
prometheus-fastapi-instrumentator==7.0.0
# SPAN margin engine (MIT, marketcalls/marginism). Pure-Python CME-SPAN .spn
# parser + NSCCL margin algorithm; wrapped behind app/marginism_adapter.py.
marginism==0.1.1
