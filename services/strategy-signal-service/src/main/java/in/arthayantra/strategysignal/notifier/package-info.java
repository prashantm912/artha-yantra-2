/**
 * Notifier module — STUB this stage (declared per §C-2.14). ntfy/Telegram signal pushes with
 * cooldown/dedup and the {@code notification_events} audit land in Stage E Phase 41; the
 * per-strategy opt-in columns already exist on {@code strategies} (Phase 21 migration).
 */
package in.arthayantra.strategysignal.notifier;
